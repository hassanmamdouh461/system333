# BrewMaster — Cloudflare D1 Sync Configuration
# ─────────────────────────────────────────────
# Central API (Cloudflare Worker → D1 database brewmaster-db)
VITE_CF_WORKER_URL=https://api.engaz.tech

# Shared branch key — same value for all POS desktop installs.
# The manager web portal bakes this in at build time.
VITE_CF_WORKER_API_KEY=brewmaster-pos-2026
