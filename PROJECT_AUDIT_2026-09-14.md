# تقرير الفحص الشامل — مشروع Engaz POS

**تاريخ الفحص:** 14 سبتمبر 2026
**المسار:** `C:\Users\Lenovo\system333`
**الفرع:** `fix/remove-hardcoded-seeds-and-sync-improvements`
**آخر commit:** `6744d20 fix: remove leaked API key from public bundle, scope repositories by branch, repair launcher`

---

## ✅ تحديث: نتائج المرحلة الأولى أُصلحت في نفس الجلسة

| البند | الحالة | الملف |
|---|---|---|
| 4-أ تسريب المخزون عند الإلغاء | ✅ **مُصلح** + اختباران regression | `electron/InventoryRepository.cjs` |
| 4-ب CI فاشل (`npm audit`) | ✅ **مُصلح** + توثيق الدين | `.github/workflows/ci.yml`، `README.md` |
| 4-ج حذف بلا حارس LWW | ✅ **مُصلح** + اختباران | `cloudflare/d1-proxy-worker.js` |
| جديد: بناء البوابة غير قابل للتكرار | ✅ **مُصلح** + اختبار | `scripts/build-reports.mjs` |

## ✅ تحديث 2: المرحلتان الثانية والثالثة أُصلحتا في نفس الجلسة

**المرحلة الثانية — نطاق الفروع والأموال:**

| البند | الحالة | الملف |
|---|---|---|
| نطاق الفرع مفقود في المنيو والمخزون | ✅ **مُصلح** + 6 اختبارات | `electron/MenuRepository.cjs`، `InventoryRepository.cjs` |
| `getDailyReportStats` يعدّ كل الفروع ويحسب الملغاة | ✅ **مُصلح** | `electron/OrderRepository.cjs` |
| مفتاح الـ API مقروء من الرندرر | ✅ **مُصلح** (كتابة فقط) | `electron/main.cjs` |
| محرك المزامنة يبتلع الأخطاء ويُجمّد العدّاد | ✅ **مُصلح** | `electron/syncEngine.cjs`، `database.cjs` |
| حساب الأموال مكرّر + سالب | ✅ **مُصلح** | `src/components/orders/POSView.tsx`، `OrderRepository.cjs` |

**المرحلة الثالثة — العاملات والبوابة والنشر:**

| البند | الحالة | الملف |
|---|---|---|
| حقول بلا حدود للحجم في عامل POS | ✅ **مُصلح** + 5 اختبارات | `cloudflare/d1-proxy-worker.js` |
| القائمة العامة تفشل مفتوحة (fail-open) | ✅ **مُصلح** + 3 اختبارات | `cloudflare/d1-reports-worker.js` |
| اقتطاع صامت عند 1000 طلب / 5000 حركة | ✅ **مُصلح** + 3 اختبارات | `cloudflare/d1-reports-worker.js` + البوابة |
| تجمّد مؤشّر التحميل + لا مهلة زمنية + أخطاء إنجليزية | ✅ **مُصلح** | `reports-site/src/App.tsx`، `api.ts` |
| CSV يعدّ الطلبات غير المدفوعة محصّلة | ✅ **مُصلح** + اختباران | `reports-site/src/csv.ts` |
| `workers_dev = true` ينشر نسخة ثانية غير مراقَبة | ✅ **مُصلح** | الأربعة `wrangler*.toml` |
| مسارات نسبية في إعداد النشر المؤقت | ✅ **مُصلح** | `deploy-reports.ps1` |

**التحقق بعد كل الإصلاحات:** lint صفر تحذيرات · `tsc` نظيف (الجذر والبوابة) · **238/238** Vitest · **27/27** Electron · **3/3** أدوات · **84/84** اختبارات العاملات · **96/96** اختبارات البوابة · بناء الجذر وبناء البوابة ناجحان مع عبور فحص الأسرار.

كل إصلاح جديد صاحبه اختبار regression **ثبت فشله على الكود القديم** قبل إعادة الإصلاح — 10 اختبارات جديدة فشلت جميعها على الكود السابق.

**التحقق بعد الإصلاح:** lint صفر تحذيرات · `tsc` نظيف (الجذر والبوابة) · **223/223** Vitest (الأساس 221) · **21/21** Electron (الأساس 19) · **3/3** أدوات (الأساس 2) · بناء الجذر وبناء البوابة ناجحان.

وتحقّقت من أن اختبار المخزون **يفشل فعلاً** على الكود القديم (أُعيد الكود مؤقتاً ففشل الاختبار، ثم أُعيد الإصلاح) — أي أنه regression test حقيقي لا يمرّ بالصدفة.

### اكتشاف جديد أثناء التنفيذ: بناء البوابة كان يفشل بشكل غير متكرر

بعد الإصلاحات الأولى فشل `cd reports-site && npm run build` في فحص الأسرار على ملف CSS — وقد **نجح** في الفحص الأول. السبب جذري ومهم:

`scripts/build-reports.mjs:34` يستدعي `loadEnv('production', dir, '')` **ببادئة فارغة**، فتُعيد Vite كل متغيرات `process.env` لا ملف `.env` وحده. ثم يُعتبر كل متغير اسمه يحتوي `KEY|SECRET|PASSWORD|TOKEN|CREDENTIAL` سرّاً. أي متغير بيئة اسمه يبدو حساساً **وقيمته قصيرة** يكفي لنسف الفحص: في هذه الجلسة كان `CODEBUDDY_API_KEY_HELPER_DISABLED=1`، و`"1"` سلسلة موجودة في أي ملف، ففشل كل بناء.

**الأثر:** بناء البوابة ينجح أو يفشل بحسب الصدفة التي تُشغّله — وعلى أي runner يضبط متغيراً مثل `NPM_TOKEN=1` يفشل النشر. **الإصلاح:** تجاهل القيم الأقصر من 8 أحرف (لا يمكن أن تكون سرّاً حقيقياً)، مع بقاء كشف الأسماء (`X-API-Key` و`VITE_*KEY`) كما هو. أُضيف اختبار يمنع الانحدار.

**بندان من المرحلة الأولى يحتاجان قرارك** (لا يمكن تنفيذهما تلقائياً): تدوير مفتاح الـ API (يحتاج `wrangler secret put` + إعادة نشر)، وتصحيح عنوان العامل في `.env` (يحتاج تأكيد أن المفتاح الحالي يطابق `api.engaz.tech`). التفاصيل في قسم 10.

---

## 1. ملخص تنفيذي

المشروع **سليم معمارياً ومستقر على مستوى البناء والاختبارات**: كل الفحوصات الآلية تمرّ بنجاح (Lint صفر تحذيرات، TypeScript نظيف، 217 اختبار Vitest + 19 اختبار Electron + اختبارات الأدوات، وبناء الحزمتين ناجح). معظم قواعد العمل موثّقة ومطبّقة، والوعي الأمني عالٍ (لا مفتاح في حزمة الواجهة، جسر IPC واحد، sandbox مفعّل، تحقق مركزي للمدخلات).

لكن الفحص كشف **عيباً وظيفياً خطيراً واحداً** (تسريب مخزون عند الإلغاء)، و**عيباً أمنياً حرجاً في الـ Worker** (مسار الحذف الناعم يتجاوز LWW)، و**عداداً من الثغرات متوسطة الخطورة** متوزّعة على طبقة المزامنة ونطاق الفروع، بالإضافة إلى **CI معطّل فعلياً** بسبب `npm audit` على Electron 29.

### تقييم سريع

| المحور | الحالة | ملاحظة |
|---|---|---|
| البناء والاختبارات | 🟢 ممتاز | كل الفحوص تمر |
| أمان حزمة الواجهة | 🟢 نظيف | صفر تسريب مفاتيح (مُتحقَّق منه ببناء فعلي وفحص ثنائي) |
| أمان Workers | 🟠 يحتاج عمل | مسار الحذف بلا حارس LWW، بلا حدود حجم |
| صحة البيانات (المخزون) | 🔴 عيب حرج | الإلغاء لا يردّ المخزون كاملاً |
| نطاق الفروع | 🟠 غير مكتمل | المخزون والقائمة والحذف اليومي بلا فلتر |
| المزامنة السحابية | 🟠 هشّة | أخطاء مبتلعة + عدّاد معطول + URL قديم |
| بوابة التقارير | 🟠 دقيقة جزئياً | اقتطاع صامت عند 1000 طلب + لا تحقق من شكل البيانات |
| النشر | 🟠 مخاطر | `workers_dev = true`، مسارات نسبية في سكربت النشر |
| CI | 🔴 فاشل | `npm audit` يخرج بـ 1 |
| التوثيق | 🟡 متأخر | `AI_HANDOFF.md` يصف حالة قديمة؛ شارة README خاطئة |

---

## 2. نتائج الفحوص الآلية (كلها ناجحة)

| الفحص | الأمر | النتيجة |
|---|---|---|
| ESLint | `npm run lint` | ✅ صفر أخطاء، صفر تحذيرات |
| TypeScript (الجذر) | `tsc --noEmit -p tsconfig.json` | ✅ نظيف |
| اختبارات الواجهة والعمال | `npm test` | ✅ **221/221** في 12 ملفاً |
| اختبارات Electron | `npm run test:electron-unit` | ✅ **19/19** على SQLite معزول |
| اختبارات الأدوات | `npm run test:tooling` | ✅ 2/2 (بصمة البناء + مسح الأسرار) |
| بناء الحزمة | `npm run build` | ✅ 627 kB (185 kB مضغوط) |
| TypeScript (البوابة) | `tsc --noEmit -p reports-site/tsconfig.json` | ✅ نظيف |
| بناء البوابة | `npm run build` | ✅ 187 kB + «Portal secret scan passed» |
| تدقيق التبعيات (إنتاج) | `npm audit --omit=dev` | ✅ 0 ثغرات |
| تدقيق التبعيات (الكل) | `npm audit` | ❌ **2 ثغرة high** (انظر 4-ب) |

**تحقّق يدوي من أخطر ادّعاء أمني:** أُجري `vite build` فعلي ثم فُحصت الحزمة الناتجة — صفر ورود لقيمة المفتاح أو لاسمه. حارس `src/services/credentialHygiene.test.ts` يعمل. هذه النقطة (P0 سابقاً) **مُصلحة فعلاً**.

---

## 3. نتائج إيجابية مؤكَّدة (لا تحتاج عمل)

- **لا SQL يُبنى من مدخلات العميل** — كل الاستعلامات نصوص ثابتة أو من خرائط مغلقة (`SYNCABLE_TABLES`، `SYNC_TABLES`).
- **كل معالجات IPC تمرّ عبر `validate.cjs`** عدا `app:print-receipt` (وهو معزول في نافذة بلا Node وبـ CSP).
- **نافذة Electron آمنة**: `nodeIntegration: false`، `contextIsolation: true`، `sandbox: true`، `webSecurity: true`، ولا تحمّل أي محتوى بعيد.
- **كلمات المرور**: PBKDF2-SHA256 بـ 210k تكرار وملح لكل جهاز، ومقارنة `timingSafeEqual` في العمال.
- **لا `any` ولا `@ts-ignore` ولا `eslint-disable`** في كود غير الاختبارات، في المشروعين.
- **الفصل بين `status` و`paymentStatus` مطبّق صحيحاً**، والإيراد يقرأ `paidAmount`.
- **حزمة البوابة نظيفة**: الرمز في `sessionStorage` لا `localStorage`، وصلاحيته مفحوصة عند القراءة، و`envPrefix` مقيّد.

---

## 4. النتائج الحرجة (🔴)

### 4-أ. الإلغاء لا يردّ المخزون إلا جزئياً — تسريب مخزون مؤكد
**الملف:** `electron/InventoryRepository.cjs:462-479`

```js
for (const movement of taken) {
  const outstanding = movement.quantity - (returnedByItem.get(movement.itemId) || 0);
  if (outstanding <= 0) continue;
```

المشكلة أن `outstanding` يُحسب مقابل **إجمالي ما رُجّع** ثم يُضاف إليه (سطر 478)، لكن حساب `outstanding` في الدورة التالية يخصم **المجموع الكلي** لا «ما تبقّى من هذا السطر».

**سيناريو الفشل:** طلب فيه سطران لنفس الصنف (`OUT 5` و`OUT 5`) → الإلغاء يردّ 5 فقط، والثاني يحسب `5-5=0` فيُتخطّى. نفس الفشل في: بيع → إلغاء → إحياء → إلغاء.

**الأثر:** كل إلغاء لطلب يحتوي صنفاً مكرراً **يُبقي جزءاً من المخزون مُخصوماً نهائياً**، فيتآكل الرصيد مع الوقت وتظهر أصناف «نافدة» وهي غير ذلك، ويختلّ COGS.

**الإصلاح:** عدّاد `remainingCredit` مستقل:
```js
let remainingCredit = movement.quantity;
for (const m of taken) {
  const take = Math.min(m.quantity, remainingCredit);
  if (take <= 0) continue;
  remainingCredit -= take;
  // ... استخدم take
}
```
> **أولوية قصوى** — هذا عيب مالي تشغيلي يحدث يومياً. وأضف اختباراً: سطران لنفس الصنف ثم إلغاء.

---

### 4-ب. CI فاشل على كل push — `npm audit` يخرج بـ 1
**الملف:** `.github/workflows/ci.yml:29-30` (و:`65-66` للبوابة)

```
- name: Audit locked dependencies
  run: npm audit
```

`npm audit` (بلا `--omit=dev`) يخرج بالرمز **1** لوجودElectron 29.4.6 المقفول في `package-lock.json` (13 إشعاراً، منها 2 high عبر `extract-zip`). لا يوجد `.npmrc` ولا `overrides` يعالج ذلك.

**الأثر:** كل PR وكل دفع إلى `main` يُعلَّم أحمر، فتُعتاد رؤية الفشل وتُتجاهل — وهذا يفرّغ CI من قيمته.

**الإصلاح (اختر واحداً):**
1. `npm audit --omit=dev` (التبعية المعنية هي أداة تطوير/تغليف) — الأسرع والأنسب.
2. أو `npm audit --audit-level=critical` مع تبرير موثّق.
3. أو `continue-on-error: true` مع خطوة منفصلة تنشر التقرير.

> ملاحظة: ترقية Electron إلى 44 **غير ممكنة عملياً** (better-sqlite3 بلا prebuild لـ ABI 149 على Windows). المسار (1) هو الصحيح، مع توثيق الدين في README.

---

### 4-ج. مسار الحذف الناعم في عامل POS يتجاوز حارس LWW بالكامل
**الملف:** `cloudflare/d1-proxy-worker.js:390-397`

```js
if (!spec.appendOnly && isDeleted(record)) {
  db.prepare(`UPDATE ${spec.table} SET deleted_at = ?, updated_at = ? WHERE id = ?`)
```

كل عمليات الـ upsert الأخرى محصّنة بـ `WHERE excluded.updated_at > …` (الأسطر 207، 249، 288، 313) — **إلا هذا المسار**. فهو يوسم الحذف بلا أي شرط زمني.

**الأثر:** فرع كان offline (أو أي انحراف ساعة) يعيد مزامنة حذف قديم → يمسح صفاً عدّله فرع آخر **بعده**. الصف يختفي من نقاط البيع ومن كل السحب. عامل التقارير لا يحتوي هذا المسار (يحذف عبر الـ upsert المحصّن) → **القاعدتان تنفصلان**.

**الإصلاح:** أضف `AND (updated_at IS NULL OR ? > updated_at)` باستخدام `updatedAt` الوارد.

---

## 5. نتائج عالية الخطورة (🟠)

### 5-1. الواجهة تستطيع توسيع نطاق الفرع ليشمل السلسلة كلها
`electron/InventoryRepository.cjs:26-31` يقبل `'manager'` و`'default'`، و`main.cjs:227-243` يمرّر قيمة الواجهة مباشرة. إرسال `'manager'` يجعل `getInventory` (سطر 42) ينفّذ `SELECT * FROM inventory WHERE deleted_at IS NULL` — **كل الفروع**.
`OrderRepository.resolveBranch` و`CashierRepository.getCashiers` يرفضان ذلك correctly؛ المخزون هو الشاذ.
**الإصلاح:** احذف `'manager'`/`'default'` من `resolveBranch` واجعل النطاق فرع الجهاز فقط.

### 5-2. الإحصاء اليومي بلا فلتر فرع ولا استبعاد للملغي
`electron/OrderRepository.cjs:545-563` — الاستعلامان يفتقدان `(branch_id = ? OR branch_id IS NULL)` ولا يستثنيان `status = 'Cancelled'`. تُرسل هذه الأرقام إلى تقرير Telegram (`telegramService.cjs:128`).
**الأثر:** في التشغيل متعدد الفروع، «إيراد اليوم» و«عدد الطلبات» يجمعان كل الفروع.

### 5-3. كتابات القائمة والمخزون غير موسومة بالفرع
- `MenuRepository.cjs:97` — `item.branchId || this.getBranchId()` (قيمة من الواجهة؛ نظيرا المخزون والعملاء يشترطان المساواة).
- `MenuRepository.cjs:143, 151, 161` و`InventoryRepository.cjs:166-198` — `WHERE id = ?` بلا شرط فرع → تعديل/حذف صف فرع آخر ممكن بأي معرّف.

### 5-4. مفتاح الـ API السحابي وبيانات المدير مقروءان من الواجهة
`electron/main.cjs:197-199` تُدرج `engaz_admin_creds` و`engaz_d1_worker_api_key` في `SETTINGS_WHITELIST`، و`db:get-settings` (205-212) يُرجعهما. وبما أن `mockApiService.cjs:64` يعتمد `engaz_d1_worker_url` من الإعدادات، فأي ثغرة حقن في الواجهة (أو XSS) تستطيع **قراءة المفتاح وإعادة توجيه المزامنة إلى خادم مهاجم**.
**الإصلاح:** قسّم القائمة إلى «قابل للكتابة فقط» (يُحذف من `get-settings`) و«قابل للقراءة».

### 5-5. فشل مزامنة المخزون والولاء يُبلَّغ كنجاح
`electron/syncEngine.cjs:340-342` — `catch` يكتفي بـ `console.warn` ولا يضيف إلى `phaseErrors`، فتنتهي الدورة بـ `state='synced'` و`lastError=null`. **المخزون والنقاط يتوقفان عن المزامنة بصمت.**

### 5-6. `getSyncStats` يعدّ الصفوف الموقوفة → المزامنة لا تتقارب أبداً
`database.cjs:552-580` مقابل `syncEngine.cjs:351-362` — العدّ بـ `is_synced = 0` بلا `sync_attempts < MAX_SYNC_ATTEMPTS`. الصفوف المتجاوزة لحدّ المحاولات تُستبعد من كل دفعة دفع، فـ `totalPending` لا يصل صفراً أبداً → الحالة تبقى `'syncing'` و`consecutiveFailures` يبقى صفراً → **لا backoff، واستقصاء كل 30 ثانية للأبد بلا تقدّم**.
كذلك `syncEngine.cjs:326` يتجاهل ميزانية المحاولات، وسطر 331 ينسى `sync_attempts = 0`.

### 5-7. الاستبدال الزائد للنقاط يُنتج إيراداً سالباً
`OrderRepository.cjs:94-96` — `roundMoney(grandTotal - pointsRedeemed)` بلا حدّ أدنى، بينما `completeOrderPayment` (292) يستخدم `MAX(0, …)`. فاتورة 50 بـ 1000 نقطة → `paidAmount = -950` يُجمع في الإيراد.

### 5-8. عامل التقارير: لا حدود لحجم الحقول
`d1-proxy-worker.js:215, 255` و`d1-reports-worker.js:279, 320, 384` — `image` و`items` و`description` بلا سقف، ولا فحص لـ `Content-Length`. عند `MAX_BATCH=200` يمكن لطلب واحد مصادَق عليه دفع عشرات الميجابايات إلى D1.
**الإصلاح:** ارفض `Content-Length` فوق ~2 MB، وضع سقفاً لكل حقل.

### 5-9. المنيو العام «يفشل مفتوحاً» عند تعذّر قراءة الإعدادات
`d1-reports-worker.js:487` — `if (!config) return { menuItems: rows, config: null }`. عند تلف JSON الإعدادات تُرسل **كل الأصناف والتصنيفات المخفية** إلى مسار عام بلا مصادقة — وهذا ينقض الوعد الصريح في التصميم (التعليق في السطر 471-474 يقول العكس).
**الإصلاح:** عند تعذّر القراءة، أرجع قائمة فارغة (fail closed) أو انقل المخفيات إلى عمود لا إلى داخل الـ blob.

### 5-10. كل نشر يُصدر أيضاً نطاق `*.workers.dev`
`wrangler-reports.toml:15`، `wrangler-reports-site.toml:12`، `wrangler-menu-site.toml:9` — `workers_dev = true` مع وجود `routes`، ولا `[env.*]` في أي ملف.
**الأثر:** أصل ثانٍ غير محمي بـ WAF يخدم `/read/public-menu` و`/branches/save`، ولا توجد بيئة staging — كل نشر هو إنتاج.
**الإصلاح:** `workers_dev = false` + `[env.staging]` بربط D1 خاص.

### 5-11. سكربت النشر ينشر من إعداد في `%TEMP%` بمسارات نسبية
`deploy-reports.ps1:40-43` ينسخ الإعداد إلى المجلد المؤقت ويمرّره بـ `-c`، بينما `main = "cloudflare/d1-reports-worker.js"` و`directory = "reports-site/dist"` مسارات نسبية — وwrangler يحلّها نسبةً إلى **مجلد الإعداد** لا مجلد العمل. النتيجة المحتملة: فشل صريح أو رفع أصول فارغة.
**الإصلاح:** أبقِ الإعداد داخل الشجرة ومرّر `database_id` عبر `--var`، أو اكتب مسارات مطلقة في النسخة المؤقتة.

### 5-12. الواجهة تحسب المال بـ float خام خارج الدالة المرجعية
`src/components/orders/POSView.tsx:359-365` و`:376`:
```js
const totalAmount = invoiceItems.reduce((sum, item) => sum + item.price * item.quantity, 0);
const taxAmount = totalAmount * taxRate;
const grandTotal = totalAmount + taxAmount;
```
بلا `roundMoney`، ويتجاهل `buildOrderTotals` المرجعية (`utils/orderTotals.ts:59`) التي تُقرّب **لكل سطر → المجموع → الضريبة → الإجمالي**. فشاشة الكاشير تعمل بخط تقريب ثانٍ مختلف عمّا يُخزَّن في اللقطة (`src/pages/Orders.tsx:28`)، و`paidAmount` يُولَّد من القيمة غير المقرّبة (`:507`، `:561`).
**الأثر:** فرق قرش بين المعروض والمحفوظ والمطبوع؛ و`printReceipts.ts:82` يطبع سطر «خصم نقاط ولاء» وهمياً على فاتورة نقدية كاملة الدفع.
**الإصلاح:** استبدل الثلاثة بـ `buildOrderTotals(invoiceItems, getTaxRate())` وغلّف الباقي بـ `roundMoney`.

### 5-13. `loading` في البوابة يلتصق على `true` إلى الأبد
`reports-site/src/App.tsx:137` + `:158`:
```js
if (!options.silent) setLoading(true);
...
if (id === requestId.current && !options.silent) setLoading(false);
```
تحديث يدوي (id=1، `loading=true`) يتجاوزه استقصاء صامت (id=2) لا يمكنه أبداً مسح الحالة: في `finally` الخاصة بـ 1 الشرط `1 === 2` خاطئ، وفي الخاصة بـ 2 الشرط `!silent` خاطئ. **النتيجة:** زر التحديث معطّل permanently وسط «جارٍ التحديث…».
**الإصلاح:** تتبّع معرّف الطلب غير الصامت المعلَّق، أو امسح `loading` عند أحدث طلب بغض النظر عن `silent`.

### 5-14. البوابة: «كل الفترات» مقتطعة صامتاً عند 1000 طلب / 5000 حركة
`d1-reports-worker.js:40,45` + `415-421` (كل استعلام `LIMIT ?`)، والواجهة `App.tsx:139-147` لا تتلقّى أي عدّاد أو علامة اقتطاع، و`analytics.ts:122` يجعل `'all'` يمرّر كل شيء.
**الأثر:** الإيراد ينقص بعد 1000 طلب؛ و**COGS أسوأ** — لأن الحركات مرتّبة من الأحدث، فطلبات قديمة داخل النطاق لا تجد سطر دفتر ويُتخطّاها (`analytics.ts:196`) → COGS يقترب من صفر بينما الإيراد حقيقي → `netProfit` و`marginPercent` و`expectedProfit` كلها **متضخّمة**. والشاشة تطبع «كل الفترات» بلا أي تحذير.
**الإصلاح:** أرجع `counts`/`truncated` من العامل واعرض شريط تنبيه، أو aggregate في الخادم.

---

## 6. نتائج متوسطة (🟡)

| # | الملف | المشكلة |
|---|---|---|
| 6-1 | `OrderRepository.cjs:188-230` | `updateOrder` يقبل `items` و`status` بلا تسوية مخزون، بينما `updateOrderStatus` (240) يسوّي → مساران متناقضان. سطر 253 `UPDATE orders SET status = ?` بلا شرط فرع/حذف |
| 6-2 | `syncEngine.cjs:247,275,289,306,320,337` | حارس النسخة (snapshots) مُمرَّر للكاشيرات فقط (261) → تعديل متزامن أثناء دفع فاشل仍 يُخصم من ميزانية المحاولات |
| 6-3 | `OrderRepository.cjs:119 vs 176` | القيمة المُعادة `totalAmount: grandTotal` تخالف ما يُخزَّن (`order.totalAmount` قبل الضريبة)؛ و`'Pending'` في سطر 116 ليست حالة قانونية |
| 6-4 | `InventoryRepository.cjs` + `syncEngine` | **جداول الدفتر لا تتقارب أبداً**: لا سحب لـ `inventory_transactions`/`points_transactions`، بينما `upsertPulledInventory:71` يستبدل `stock` من السحابة → الأرصدة والدفاتر تنفصل، فقاعدة «الإلغاء يردّ من الدفتر» تردّ الكمية الخطأ |
| 6-5 | `d1-proxy-worker.js:217,270,295,321,337,356,374` | معرّفات الفروع بلا تحقق من النمط (العامل الآخر يتحقق منه فقط، ويسمح بـ `_` خلاف المواصفة). `"Main Branch"` أو مسافة زائدة تختم صفوفاً لا يطابقها أي فلتر |
| 6-6 | `d1-proxy-worker.js:456,482` | ترقيم الصفحات بالمؤشر يتعطل على `updated_at` الفارغ: `str(null)` يُفشل اختبار `!= null` فيعيد الاستعلام من الصفحة 1 للأبد؛ و`updated_at >= ?` (460) لا يجلب الصفوف الفارغة أصلاً. `menu_items`/`inventory`/`cashiers` لم تُعبَّأ بـ backfill |
| 6-7 | `d1-proxy-worker.js:465` | `/pull/orders` يسلّم الصفوف بلا فرع لكل الفروع (صحيح للجداول المشتركة، خطأ للطلبات) |
| 6-8 | `d1-proxy-worker.js:388`، `d1-reports-worker.js:769` | أخطاء تحقق من العميل تُرجع **500** بدل 400 (نمط الرسالة لا يطابق الاستدلال في 682) |
| 6-9 | `main.cjs` | لا توجد Error Boundary في التطبيق كله — أي استثناء أثناء الرسم يُبيّض الشاشة أثناء الخدمة، والسلة المعلَّقة لا تُستعاد |
| 6-10 | `src/context/AuthContext.tsx:186` + `App.tsx:20` | `ProtectedRoute` بوابة جانب العميل فقط (`!!session` من `localStorage`) على حزمة منشورة للعامة. محتواها اليوم لأن كل مسار بيانات يمرّ بـ `electronAPI` الغائب على الويب — لكنها لا تضمن شيئاً |
| 6-11 | `reports-site/src/App.tsx:141-146` | `as unknown as` بلا أي تحقق وقت التشغيل من شكل JSON العامل. `toNum` يحوّل الغائب إلى 0 → لوحة أصفار لا تُميَّز عن لوحة حقيقية |
| 6-12 | `reports-site/src/api.ts:46-59` | لا `AbortController`/مهلة، لا إعادة محاولة/تراجع، و`res.json()` يرمي `SyntaxError` على أي صفحة خطأ HTML تُعرض للمستخدم حرفياً (`App.tsx:389`)؛ و429 لا يوقف الاستقصاء |
| 6-13 | `reports-site/src/analytics.ts:202` | `Math.max(total, 0)` يخفي إشارة COGS خاطئة عالمياً؛ و`costOfGoodsSold` لا يعترف إلا بـ `'OUT'`/`'IN'` بحرفية → أي منتج يكتب حروفاً صغيرة يُصفّر التكلفة |
| 6-14 | `reports-site/src/csv.ts:140` و`AnalyticsTab.tsx:40` | التصدير وأفضل الأصناف مبيعاً يحسبان الطلبات غير المدفوعة، بينما بطاقة الإيراد لكل فرع (`analytics.ts:365`) تصفيها → رقم التصدير يخالف المعروض على الشاشة |
| 6-15 | `database.cjs` | فجوات ترحيل: 0011 (87-89) و0010 (444-446) تبتلع الأخطاء؛ 0010 يعمل **بعد** 0012/0013/0014 رغم رقمه؛ و`isMigrationApplied` (490) يفشل مغلقاً فيتخطى كل الترحيلات عند خطأ قراءة عابر. تعليق 289-290 يضيف `deleted_at` فقط بلا `updated_at` → جداول الدفتر لا يمكن حراستها بنسخة |
| 6-16 | `mockApiService.cjs:149` | في وضع التقارير فقط يُرجع `{ success: true, written: … }` مُختلَقاً، ثم تُوسم الصفوف كمزامَنة |
| 6-17 | `wrangler.toml:35` / `wrangler-menu-site.toml:5` | `npx wrangler` بلا تثبيت إصدار، و`node ./node_modules/wrangler/wrangler-dist/cli.js` مسار غير موجود. عامل POS الإنتاجي **بلا مسار نشر ثابت** |
| 6-18 | `wrangler-reports.toml:30` | معرّف قاعدة حقيقية مُودَع في المستودع، بينما `wrangler.toml:16-27` يوثّق عكس ذلك — سياستان متناقضتان |
| 6-19 | `d1-reports-worker.js:52,141-146` | رموز القراءة 8 ساعات بلا `jti` ولا ربط بـ IP ولا إبطال |
| 6-20 | `package.json` (الجذر والبوابة) | `engines: node >=24.16.0 <25` و`npm >=11 <12` تثبيت صارم سيكسر CI على أي Node 22/23 أو 25 لاحقاً |

---

## 7. نتائج منخفضة وجودة الكود (⚪)

- **حجم الملفات:** `POSView.tsx` **1296** سطراً، `Inventory.tsx` 788. استخرج `hooks/useCashiers.ts` و`CashierModal.tsx` (~180 سطراً ويجعل منطق السباق الكاشيري قابلاً للاختبار).
- **كود ميت:** `src/hooks/useSwipe.ts:17` بلا أي مستورد؛ `workerUrl()` في `workerClient.ts:16` مُصدَّر بلا مستهلك؛ `mockApiService.deleteMenuItem:357` و`pullCashiers:467` و`database.peekDailyOrderNumber:599` بلا مستدعِين.
- **تكرار:** `nextUpdatedAt` منسوخ في 5 مستودعات؛ `sync_attempts < 5` مكتوب يدوياً في 5 مواضع بدل `MAX_SYNC_ATTEMPTS`.
- **أداء حلقة الـ 30 ثانية:** `getDailyReportStats` يطبّق `date(...,'localtime')` على كل صف (مسح كامل غير قابل للفهرسة) ثم يوزّع JSON لكل طلب في JS؛ و`getOrders:68` ينفّذ `SELECT *` بـ `ORDER BY CAST(...)` بلا `LIMIT` عند كل تحديث للواجهة؛ و`getBranchId` استعلام لكل نداء مستودع.
- **إتاحة الوصول:** أزرار الكمية والحذف في `POSView.tsx:1056-1072` أيقونات بلا `aria-label`؛ ولا مسار لوحة مفاتيح للّوحة الرقمية.
- **لا CSP** في `index.html` (موجود فقط في نافذة الطباعة `printManager.cjs:59`) — defense in depth مع `sandbox` مُفعّل أصلاً.
- **رأس CORS:** `d1-proxy-worker.js:67` يعلن `Allow-Methods: POST, OPTIONS` بينما `/health` هو GET (583) → preflight المتصفح لـ GET يفشل. ولا `X-Content-Type-Options: nosniff` في أي عامل.

---

## 8. فجوات التغطية الاختبارية

217 اختباراً تمر، لكنها **تركّز على الدوال النقية**. لا يوجد ملف اختبار في `cloudflare/__tests__/` يستدعي `fetch` (بحث `.fetch(` يُرجع صفراً) — أي أن **سطح المصادقة والتفويض كله غير مختبر**:

| غير مختبر | الأهمية |
|---|---|
| 401 عند غياب/خطأ `X-API-Key`، 503 عند غياب إعداد الزائر | عالية |
| **أن رمز القراءة يصل `/read/snapshot` و`/branches/save` ولا يصل `/sync/*` أو `/migrate`** | **أهم خاصية أمنية في النظام** |
| تفعيل تحديد المعدل (429) و304 عبر `fetch` | عالية |
| `pullTableWithCursor` (مُصدَّر في `__testing` بصفر اختبارات) | عالية (يخفي عيب 6-6) |
| شكل SQL للحذف (يُختبر شكلاً فقط، بلا حارس LWW) | عالية (يخفي 4-ج) |
| `reports-site/src/api.ts` بالكامل: 401/429/انتهاء الصلاحية/أجسام غير JSON | عالية |
| تسلسل الطلبات في `App.tsx` (عيب 5-13) وإيقاف الاستقصاء عند إخفاء التبويب | متوسطة |
| أي مكوّن React — لا jsdom ولا `@testing-library` في البوابة، ولا كتلة `test` في `vite.config.ts` | متوسطة |
| لا يوجد `wrangler deploy --dry-run` في CI → خطأ ربط أو صياغة يظهر أولاً عند النشر | متوسطة |

**أهم اختبار مفقود على الإطلاق:** حالة إلغاء طلب فيه سطران لنفس الصنف (4-أ).

---

## 9. تناقضات التوثيق

| الملف | المشكلة |
|---|---|
| `README.md:10` | الشارة تقول **Electron 44** بينما الفعلي **29.4.6** (`package.json:65`). مضلّل جداً لمن يقرأه |
| `AI_HANDOFF.md:14-15` | يقول الفرع فيه «شغل غير ملتزم» وآخر commit `15a916a` — **متأخر**: الشجرة الآن نظيفة وآخر commit `6744d20`. وصفه للحالة (قسم 9) لم يعد مطابقاً |
| `AI_HANDOFF.md:91` | يقول `VITE_REPORTS_API_KEY` غير مضبوط — صحيح (تحقّقت منه)، لكن `.env` يحتوي **`VITE_CF_WORKER_URL=https://brewmaster-d1-proxy.hassanmamdouh461.workers.dev`** — **وهذا ليس خطأً**: تحقّقت أن `engaz-d1-proxy` غير منشور أصلاً (code 10007) وأن `api.engaz.tech` لا يردّ ردّ العامل. التوصية القديمة بتغييره كانت ستكسر المزامنة؛ التفاصيل في قسم 10 |
| `README.md:151` | يقول CI يشغّل «نفس المجموعات» — صحيح، لكنه لا يذكر أن `npm audit` يُسقطه (4-ب) |
| `AGENTS.md` + `AI_HANDOFF.md` | اثنان من ثلاثة ملفات توجيه؛ محتواهما متداخل ويختلف في التفاصيل. يفضّل دمجهما |

---

## 10. خطة عمل مرتّبة بالأولوية

### المرحلة 1 — فورية (تؤثر على المال أو الأمن، اليوم)
1. **إصلاح ردّ المخزون عند الإلغاء** (4-أ) + اختبار regression.
2. **إصلاح `npm audit` في CI** (4-ب) — `--omit=dev`، ووثّق دين Electron.
3. **إضافة حارس LWW لمسار الحذف** في عامل POS (4-ج) + إعادة نشره.
4. **تدوير `VITE_CF_WORKER_API_KEY`** (الحزمة القديمة كانت منشورة على `menu.engaz.tech` — بند مفتوح من قبل ولم يُغلق) ثم تحديث `.env` وإعادة النشر.
5. **تصحيح `engaz_d1_worker_url` إلى `https://api.engaz.tech`** — المزامنة معطّلة حتى ذلك.

### المرحلة 2 — هذا الأسبوع (صحة البيانات والنطاق)
6. توحيد نطاق الفرع: حذف `'manager'`/`'default'` من المخزون، وإضافة الفلتر للقائمة والإحصاء اليومي (5-1، 5-2، 5-3).
7. إخراج `engaz_d1_worker_api_key` و`engaz_admin_creds` من القراءة في `db:get-settings` (5-4).
8. معالجة أخطاء مزامنة المخزون/النقاط وإصلاح `getSyncStats` (5-5، 5-6).
9. تثبيت حساب المال في `POSView` على `buildOrderTotals` (5-12).
10. `MAX(0, …)` لمنطق الاستبدال الزائد (5-7).

### المرحلة 3 — الأسبوعان القادمان (العمال والبوابة) — ✅ مُنجزة
11. ~~حدود الحجم وفحص `Content-Length` في العاملين (5-8)~~ → ✅ مُنجز.
12. ~~جعل المنيو العام يفشل مغلقاً (5-9)~~ → ✅ مُنجز.
13. ~~`workers_dev = false` (5-10) وإصلاح مسارات سكربت النشر (5-11)~~ → ✅ مُنجز (بيئة staging ما زالت مطلوبة).
14. ~~البوابة: إصلاح التصاق `loading` (5-13)، وإظهار الاقتطاع (5-14)، وإضافة مهلة/تراجع (6-11، 6-12)~~ → ✅ مُنجز.
15. ~~توحيد حساب الطلبات غير المدفوعة بين التصدير والشاشة (6-14)~~ → ✅ مُنجز.

**ما بعد التنفيذ — بندان يحتاجان نشراً يدوياً:**
- إعادة نشر العاملين ليصبح `workers_dev = false` فعّالاً على الحساب:
  `npx wrangler deploy -c wrangler-reports.toml` ثم `-c wrangler-reports-site.toml` و`-c wrangler-menu-site.toml`.
  ويفضّل حذف نُسخ `*.workers.dev` القديمة من لوحة Cloudflare إن كانت منشورة فعلاً.
- حقل `truncated` الجديد لن يظهر قبل إعادة نشر عامل التقارير؛ البوابة تتعامل مع غيابه كـ«لا اقتطاع»، فلا انكسار، لكن التحذير لن يعمل حتى النشر.

---

## ⚠️ تصحيح جوهري: البند 5 من المرحلة 1 كان **سيكسر المزامنة**

أثناء محاولة نشر عامل POS اكتشفت إن التوصية الأصلية خاطئة، وأصححها هنا صراحةً لأن تنفيذها كان هيوقف المزامنة تماماً.

### ما قيل في البند 5 (خاطئ)
> «تصحيح `engaz_d1_worker_url` إلى `https://api.engaz.tech` — المزامنة معطّلة حتى ذلك.»

### الحقيقة بعد الفحص على الحساب الفعلي (14 سبتمبر، بالأدلة)

| الفحص | النتيجة |
|---|---|
| `wrangler deployments list --name engaz-d1-proxy` | ❌ **`This Worker does not exist on your account` (code 10007)** — لم يُنشر قط |
| `curl https://api.engaz.tech/health` | `401 {"error":"Unauthorized","message":"Missing or invalid session"}` — **ليس** ردّ العامل (العامل يردّ `{"ok":true,"service":"engaz-d1-proxy"}`) |
| `curl https://brewmaster-d1-proxy...workers.dev/health` | ✅ `200 {"ok":true,"service":"brewmaster-d1-proxy"}` |
| `wrangler d1 list` | **لا توجد قاعدة اسمها `engaz-db`**. الموجود: `engaz-reports-db`، `system-online-db`، `brewmaster-db` |
| جداول `brewmaster-db` | `api_keys, customers, inventory, menu_items, orders, users` — **38 طلب** |
| جداول `system-online-db` | `auth_users, companies, customers, inventory, inventory_transactions, login_attempts, menu_items, orders, recipes, settings, snapshots, stock_delta_ops` |

**الاستنتاج:** عامل POS الحيّ فعلاً هو `brewmaster-d1-proxy` على `*.workers.dev`، وقاعدته على الأرجح `brewmaster-db`. أما `wrangler.toml` و`api.engaz.tech` فإعداد **لم يُنفَّذ قط**.

**لذلك:** عنوان `.env` الحالي (`brewmaster-d1-proxy...workers.dev`) **ليس خطأً بل السبب الوحيد الذي يجعل المزامنة تعمل**. تغييره إلى `api.engaz.tech` كان سيوجّه المزامنة إلى عامل غير موجود، فيفشل كل دفع بصمت.

### ما لم يصل إلى الإنتاج نتيجةً لذلك
إصلاحات `cloudflare/d1-proxy-worker.js` (حدود الحجم، وحارس LWW على الحذف) **ليست مطبَّقة في أي مكان حيّ** — لأنها في عامل لم يُنشر.

### الإجراء الصحيح (يحتاج تأكيدك)
لا أنفذه تلقائياً لأنه يربط عاملاً جديداً بقاعدة بيانات إنتاجية:
1. تأكيد أن `brewmaster-db` (`2ad7a77f-29a7-493a-94e5-4e43790b5c36`) هي قاعدة POS المقصودة — أم أن `system-online-db` هي الأحدث؟
2. تعبئة `database_id` في `wrangler.toml:32` بالـ id المؤكَّد.
3. ضبط سرّ `WORKER_API_KEY` على العامل الجديد (بدونه يردّ 401 على كل شيء — فشل آمن).
4. `npx wrangler deploy -c wrangler.toml`، ثم تحويل `.env` إلى `api.engaz.tech`.
5. التحقق من `api.engaz.tech` — هل الـ 401 الحالي من Cloudflare Access؟ إن كان كذلك فلن يمرّ الطلب إلا بعد إضافة سياسة تجيزه.

### المرحلة 4 — تحسين مستمر
16. Error Boundary + بوابة مسارات المشغّل (6-9، 6-10).
17. اختبارات `fetch` لعمال Cloudflare، خاصة تفويض رمز القراءة (قسم 8).
18. ترحيل آمن: إصلاح ترتيب 0010 و`isMigrationApplied`، وإضافة `updated_at` لجداول الدفتر (6-15).
19. تقسيم `POSView.tsx` وحذف الكود الميت (قسم 7).
20. تحديث `README.md` و`AI_HANDOFF.md` ودمج ملفات التوجيه (قسم 9).

---

## 11. ملاحظة ختامية

المشروع **ليس في حالة سيئة** — العكس صحيح: البنية سليمة، والوعي الأمني أعلى من المتوسط، والفحوص الآلية كلها خضراء. المشكلة الحقيقية ليست في التصميم بل في **التنفيذ غير المكتمل لقاعدتين كتبهما المشروع بنفسه**: نطاق الفرع، وحارس النسخة عند المزامنة. أضف إلى ذلك **CI أحمر دائم** يُضعف قيمة كل فحص، و**مزامنة موجهة إلى عنوان قديم** تجعل كل اختبارات المزامنة النظرية بلا قيمة عملية.

البدء من المرحلة 1 يعالج أخطر ما في النظام بأقل جهد.
