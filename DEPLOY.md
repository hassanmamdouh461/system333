# Engaz Reports Portal — Deploy Guide

موقع التقارير على العنوان التالي:

```text
reporting.engaz.tech
```

يقرأ من قاعدة بيانات معزولة تمامًا عن قاعدة نقاط البيع الإنتاجية، فأي مشروع جديد على نطاق فرعي آخر لا يمسّه.

## المفاتيح المطلوبة

الموقع لا يحمل أي مفتاح: هو صفحة ثابتة، وأي قيمة داخل حزمتها تكون علنية. الزائر يسجّل الدخول بكلمة مرور، ويعيد له العامل رمز قراءة قصير الأجل.

| المفتاح | مَن يحمله | الغرض |
|---|---|---|
| `CLOUDFLARE_API_TOKEN` | ملف البيئة في الجذر، للنشر فقط | صلاحيات النشر على النطاق |
| `REPORTS_API_KEY` | سرّ العامل، ونسخته في سطح المكتب | الكتابة، لتطبيق سطح المكتب وحده |
| `REPORTS_VIEWER_PASSWORD` | سرّ العامل | كلمة مرور دخول الزائر |
| `REPORTS_TOKEN_SECRET` | سرّ العامل | توقيع رموز القراءة |

في ملف البيئة بالجذر:

```bash
CLOUDFLARE_API_TOKEN=your_cloudflare_token
VITE_REPORTS_API_KEY=your_strong_write_key
REPORTS_VIEWER_PASSWORD=your_viewer_password
REPORTS_TOKEN_SECRET=your_long_random_secret
```

في ملف بيئة الموقع، العنوان وحده:

```bash
VITE_REPORTS_WORKER_URL=https://api-reports.engaz.tech
```

## النشر

```bash
powershell -ExecutionPolicy Bypass -File deploy-reports.ps1
```

السكربت يفحص المصادقة، ينشئ القاعدة إن لم تكن موجودة، يضبط الأسرار، ينشر عامل التقارير، ثم يبني الموقع وينشره ويشغّل التهجير. كل خطوة تفحص رمز الخروج، فالبناء الفاشل لا ينشر النسخة القديمة.

أداة النشر ليست من تبعيات المشروع، فالسكربت يجلبها عند التشغيل. لتثبيتها مؤقتًا بدون تعديل ملف الحزم:

```bash
npm install --no-save wrangler@4.42.0
```

للنشر خطوة بخطوة بدلًا من السكربت، من جذر المشروع:

```bash
./node_modules/.bin/wrangler deploy -c wrangler-reports.toml
cd reports-site && npm run build && cd ..
./node_modules/.bin/wrangler deploy -c wrangler-reports-site.toml
```

## تدوير الأسرار

أي قيمة تظهر داخل حزمة الموقع المنشورة تُعتبر محروقة ولا يكفي حجبها، لأن الحزمة القديمة قد تكون محفوظة في ذاكرة وسيطة أو لدى زائر. الترتيب الصحيح هو رفع السرّ الجديد إلى العامل أولًا، ثم تحديث ملف البيئة، ثم إعادة النشر:

```bash
./node_modules/.bin/wrangler secret put REPORTS_API_KEY -c wrangler-reports.toml
./node_modules/.bin/wrangler secret put REPORTS_VIEWER_PASSWORD -c wrangler-reports.toml
./node_modules/.bin/wrangler secret put REPORTS_TOKEN_SECRET -c wrangler-reports.toml
```

تدوير مفتاح الكتابة يوقف مزامنة تطبيق سطح المكتب حتى تُحدَّث نسخته من المفتاح في ملف البيئة بالجذر. وتدوير سرّ التوقيع يُخرج كل الزوّار الحاليين من جلساتهم فورًا.

للتحقق من الأسرار المرفوعة حاليًا دون كشف قيمها:

```bash
./node_modules/.bin/wrangler secret list -c wrangler-reports.toml
```

### مفتاح الكتابة الحالي محروق ويجب تدويره

حزمة منيو العملاء المنشورة على النطاق التالي كانت تحمل مفتاح الكتابة كاملًا:

```text
menu.engaz.tech
```

سببه أن نشر إعدادات المنيو كان يقرأ المفتاح في المتصفح من المتغير التالي، فأدرجه البناء داخل الحزمة:

```text
VITE_REPORTS_API_KEY
```

النشر انتقل الآن إلى العملية الرئيسية لسطح المكتب، وحزمة البناء الجديدة لا تحمل أي مفتاح. لكن الحزمة القديمة كانت متاحة للتنزيل من الإنترنت، ومن نزّلها يملك مفتاحًا يكتب في قاعدة التقارير. لذلك الترتيب التالي واجب لا اختياري:

- ارفع مفتاحًا جديدًا إلى العامل بالأمر أعلاه.
- حدّث قيمته في ملف البيئة بالجذر.
- أعد بناء ونشر حزمة المنيو وحزمة الموقع بعد التحديث.
- تحقق أن الحزمة المنشورة لا تحمل المفتاح.

للتحقق من الحزمة المنشورة بعد النشر، مع وضع المفتاح في متغير بيئة بدل كتابته في الأمر:

```bash
curl -s "https://menu.engaz.tech/$(curl -s https://menu.engaz.tech/ | grep -o 'assets/index-[^"]*\.js')" | grep -c "$REPORTS_API_KEY"
```

والنتيجة المطلوبة صفر.

## نموذج الوصول

- تطبيق سطح المكتب يحمل مفتاح الكتابة، ويرسل نسخة من كل سجل إلى قاعدة التقارير.
- الزائر يفتح الموقع، يُدخل كلمة المرور، فيحصل على رمز قراءة صالح لثماني ساعات.
- رمز القراءة لا يستطيع الكتابة، ومحاولات الدخول محدودة بعشر محاولات في الدقيقة لكل عنوان.

## التحديث المباشر

اللوحة تسحب لقطة جديدة كل عشرين ثانية افتراضيًا، والمدة قابلة للتغيير من قسم الإعدادات. وتتوقف تلقائيًا عندما تكون النافذة مخفية حتى لا تستهلك طلبات على شاشة لا يقرأها أحد. وتظهر توقيت آخر تحديث بجانب النطاق المختار، وهو توقيت قراءة العامل للبيانات لا توقيت وصولها للمتصفح. ويمكن للمستخدم إيقاف التحديث التلقائي ثم تشغيله من زر في الشريط العلوي.

## أقسام اللوحة

| القسم | المحتوى |
|---|---|
| الإحصائيات | الإيرادات، المستحقات، تكلفة البضاعة المباعة، صافي الربح، مبيعات آخر سبعة أيام، الأصناف الأكثر مبيعًا، الإيرادات لكل فرع |
| القائمة | كل صنف بسعره وكميته المباعة وإيراده في الفترة، والأصناف التي لم تُبع |
| المخزون | الأصناف والمستويات وقيمة المخزون، والمنخفض منها أولًا |
| العملاء | النقاط وعدد الطلبات والإنفاق في الفترة لكل عميل |
| الإعدادات | المظهر وكثافة العرض، مدة التحديث، النطاق الافتراضي، تصدير الجداول، وصلاحية الجلسة |

فوق كل قسم مرشِّحان يعملان معًا: الفرع، والفترة الزمنية. ويُحفظ اختيارهما محليًا فيعود كما تركته.

## الإعدادات

كل ما في هذا القسم تفضيل عرض يُحفظ على جهاز المستخدم وحده، فقاعدة بيانات التقارير لا تحمل سجلًا لكل مستخدم:

- المظهر فاتح أو داكن أو تابع لإعداد نظام التشغيل، وكثافة العرض مريحة أو مضغوطة.
- مدة التحديث التلقائي، والمدة الأقصر تعني طلبات أكثر على العامل.
- الفرع والفترة اللذان تفتح عليهما اللوحة، ويمكن ربطهما بآخر اختيار.
- تصدير أي جدول من النطاق المعروض بصيغة يقرأها Excel، والملف يُبنى في المتصفح ولا يمر بأي خادم.
- استعادة كل التفضيلات إلى الوضع الافتراضي، وتسجيل الخروج.

التفضيلات محفوظة في مفتاح واحد بالمتصفح:

```text
engaz_reports_settings
```

أي قيمة غير معروفة فيه تعود إلى الافتراضي عند القراءة، فمفتاح مُحرَّر يدويًا لا يصل إلى الواجهة.

## من أين تأتي الأرقام

تكلفة البضاعة المباعة تُقرأ من سجل حركة المخزون لا من الوصفات: السجل يحفظ الكمية التي استهلكها كل طلب فعلًا، فتعديل الوصفة بعد البيع لا يغيّر تكلفة بيع ماضٍ. وحركة الإرجاع عند الإلغاء تُطرح من الاستهلاك، فالطلب الملغى لا يحمّل تكلفة.

الأرباح المتوقعة من المخزون تُقدَّر بهامش الربح الذي تحقق في الفترة المختارة نفسها. وعندما لا تكون هناك مبيعات في الفترة تظهر شرطة بدلًا من رقم، لأن التقدير بلا هامش مرصود يكون تخمينًا يُقرأ كأنه حقيقة.

## واجهات العامل

| المسار | الصلاحية | الوظيفة |
|---|---|---|
| `GET /health` | مفتوح | فحص الحياة |
| `GET /read/public-menu` | مفتوح | منيو العملاء وإعداداته |
| `POST /auth/login` | كلمة مرور الزائر | إصدار رمز قراءة |
| `POST /read/snapshot` | رمز قراءة أو مفتاح كتابة | كل بيانات الموقع في طلب واحد، ومنها سجل الفروع |
| `POST /branches/save` | رمز قراءة أو مفتاح كتابة | إضافة فرع أو تعديل هويته |
| `POST /public-menu-config` | مفتاح كتابة | نشر إعدادات المنيو |
| `POST /sync/<table>` | مفتاح كتابة | استقبال سجلات من سطح المكتب |
| `POST /migrate` | مفتاح كتابة | إنشاء الجداول |

اللقطة الواحدة تُرجع الطلبات والعملاء والمخزون وأصناف القائمة وحركة المخزون وسجل الفروع، مع توقيت قراءة العامل. وسجل حركة المخزون له حد أعلى أكبر من بقية الجداول، لأن الطلب الواحد يكتب سطرًا لكل مكوّن فيه.

سجل الفروع هو الكتابة الوحيدة التي يُجيزها رمز الزائر، لأنه لا يحمل أرقام مبيعات ولا بيانات عملاء، ولأن المدير يجب أن يفتح فرعًا دون مطوّر. تحقُّق المعرّف صارم في العامل نفسه: حروف إنجليزية صغيرة وأرقام وشرطة، لأن المعرّف يختم على كل طلب ويُقارن حرفيًا في كل مرشِّح. الفرع المتوقف يبقى بتقاريره، ويُدرَج أي معرّف يصل بياناته قبل تسجيله في بطاقة الفروع لتسميته لا لإخفائه.

مسار المنيو العام مفتوح بلا مصادقة، لأنه ما يفتحه العميل من هاتفه. والصنف المخفي أو التصنيف المخفي يُستبعد من الاستجابة نفسها لا من الصفحة وحدها، فمن يفتح المسار مباشرة لا يرى ما أخفاه صاحب المطعم.

العامل يملك كل نصوص الاستعلام؛ العميل يرسل بيانات ومرشّحات فقط.

## لوحة التحكم في المنيو

من تبويب الإعدادات في تطبيق سطح المكتب. التعديلات تُنشر إلى قاعدة التقارير، ويقرأها منيو العملاء مباشرة:

| القسم | ما يتحكم فيه |
|---|---|
| النصوص والهوية | اسم المطعم، الوصف الترحيبي، شريط إعلان، رسالة التذييل، وبيانات التواصل |
| الصور والشعار | الشعار وصورة الغلاف: رفعًا من الجهاز، أو رابطًا، أو خلفية جاهزة |
| المظهر والألوان | خمسة أنماط لونية، ورمز العملة المطبوع بعد كل سعر |
| طريقة العرض | شبكة أو قائمة، ترتيب الأصناف، وإظهار الأسعار والصور والأوصاف والعدد والبحث |
| التصنيفات | ترتيب الظهور، اسم معروض لكل تصنيف، وإخفاء تصنيف كامل |

الإخفاء قرار عرض فقط: الكاشير يظل يبيع الصنف المخفي، وحالة التوفر وحدها هي ما يمنع البيع.

الترتيب يبقى كما رتّبه صاحب المطعم، والتصنيف الذي يظهر بعد آخر نشر يُضاف في النهاية فلا يعيد خلط ما رُتِّب يدويًا.

بجانب اللوحة معاينة حية لصفحة العميل، تقرأ جدول الألوان ودوال العرض نفسها التي تقرأها الصفحة، فما يُرى في المعاينة هو ما يراه العميل.

النشر يحتاج مفتاح الكتابة، وهو محفوظ في العملية الرئيسية لسطح المكتب ولا يصل إلى المتصفح. لذلك النشر يعمل من التطبيق المثبَّت وحده؛ وفي نسخة الويب تُحفظ التعديلات على الجهاز وتظهر رسالة صريحة بأنها لم تُنشر.

الحد الأقصى لحجم الإعدادات المنشورة بصورتيها:

```text
900000 بايت
```

والقيمة المرفوضة تعود بالرمز `400` مع سبب يظهر لمن ضغط النشر، فلا تُحفظ بصمت.

## التحقق بعد النشر

الفحوص التالية تُنفَّذ من الطرفية بلا متصفح، والمتغير يحمل مفتاح الكتابة الجديد.

فحص حياة العامل، ويجب أن يعود بنجاح:

```bash
curl -s https://api-reports.engaz.tech/health
```

المفتاح القديم يجب أن يُرفض بالرمز `401`

طلب رمز قراءة بكلمة مرور الزائر:

```bash
curl -s -X POST https://api-reports.engaz.tech/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"password":"<كلمة المرور>"}'
```

رمز القراءة يجب أن يفشل على أي مسار كتابة بالرمز `401`

وأخيرًا فحص الحزمة المنشورة: نزّلها وابحث فيها عن أي سرّ، ويجب ألّا يوجد أي مطابقة ولا أي ترويسة مفتاح:

```bash
curl -s https://reporting.engaz.tech/ | grep -o 'assets/index-[^"]*\.js'
```

## البنية

| المكوّن | الملف |
|---|---|
| عامل التقارير | `cloudflare/d1-reports-worker.js` |
| عامل نقاط البيع | `cloudflare/d1-proxy-worker.js` |
| اختبارات العاملين | `cloudflare/__tests__/` |
| إعداد نشر العامل | `wrangler-reports.toml` |
| إعداد نشر الموقع | `wrangler-reports-site.toml` |
| الموقع | `reports-site/` |
| حسابات اللوحة | `reports-site/src/analytics.ts` |
| اختبارات الحسابات | `reports-site/src/analytics.test.ts` |
| تفضيلات اللوحة | `reports-site/src/settings.ts` |
| تصدير الجداول | `reports-site/src/csv.ts` |
| سكربت النشر | `deploy-reports.ps1` |
| عميل المزامنة في سطح المكتب | `electron/mockApiService.cjs` |
