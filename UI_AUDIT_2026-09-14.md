# مراجعة واجهات الاستخدام (Frontend UI Audit)

التاريخ: 2026-09-14
النطاق: `src/` (تطبيق الكاشير Electron/React) + `reports-site/` (بوابة تقارير المدير)

---

## ملخص تنفيذي

الكود فيه أساس بصري كويس جدًا: بالتة ألوان موكا/كراميل مضبوطة، عناية واضحة بالـ RTL،
تفاصيل لطيفة زي الأصوات والـ haptics والـ numpad. المشكلة الحقيقية مش في "الشكل"،
المشكلة في **ثلاث حاجات**:

1. **كود ميت كتير** — حوالي 900 سطر واجهة مش بتترسم خالص.
2. **كلاسات Tailwind مش موجودة** — ألوان بتترمي في الهوا في صفحة التقارير.
3. **تكرار في الـ panels** — 5+ أشكال مختلفة لنفس "الكارت"، و3 طرق لكتابة العملة.

بوابة التقارير (`reports-site/`) هي العكس تمامًا: أنضف حتة في المشروع،
CSS variables + `data-theme` + `data-density`. مفيهاش حاجة تتصلح غير إنها
مختلفة بصريًا عن التطبيق الرئيسي.

---

## أولوية 1 — حاجات مكسورة فعلاً (تتصلح في يوم)

### 1.1 ألوان `caramel` الرقمية مش معرّفة في Tailwind

`tailwind.config.js` بيعرّف بس:

```js
caramel: { light: '#EADBC8', DEFAULT: '#C8956C', dark: '#A67347', gold: '#D4AF37' }
```

مفيش `caramel-50 / 100 / 500 / 600 / 700`. والكود بيستخدمهم:

| الملف | السطر | الكلاس | النتيجة |
|---|---|---|---|
| `src/pages/Reports.tsx` | 348 | `text-caramel-600` | النص يبقى أسود |
| `src/pages/Reports.tsx` | 352 | `bg-caramel-50/50` | المسار (track) بدون خلفية |
| `src/pages/Reports.tsx` | 352 | `border-caramel-100/30` | بدون border |
| `src/components/ui/Card.tsx` | 80 | `bg-caramel-100`, `bg-caramel-500`, `text-caramel-700` | (ملف ميت — شوف 1.3) |

يعني شريط "Dine-in" في صفحة التقارير شكله النهائي: bar ملوّن على خلفية بيضاء
فاضية، ونص أسود بدل الكراميل. باين جدًا على الشاشة.

**التصليح:** إما ضيف scale كامل 50→900 للـ caramel في `tailwind.config.js`،
أو استبدل بـ `caramel-light` / `caramel` / `caramel-dark`.

### 1.2 `animate-fade-in` مش معرّف

مستعمل في `POSView.tsx:980` و `InvoicePanel.tsx:117` على رسالة
"اضغط على أي ترابيزة لحذفها". مفيش keyframe بالاسم ده لا في `tailwind.config.js`
ولا في `index.css`. الأنيميشن ببساطة مش موجود.
**التصليح:** ضيف الـ keyframe أو شيل الكلاس.

### 1.3 كود واجهة ميت (~900 سطر)

مفيش أي import لأي حاجة من دول:

| الملف | الأسطر | ملاحظة |
|---|---|---|
| `src/components/orders/pos/InvoicePanel.tsx` | 280 | `POSView` بيعمل نفس الحاجة inline |
| `src/components/orders/pos/PaymentPanel.tsx` | 191 | نفس الكلام |
| `src/components/orders/pos/ProductGrid.tsx` | 110 | نفس الكلام |
| `src/components/ui/Card.tsx` | 137 | `Card`/`SectionHeader`/`EmptyState`/`Meter` — مبني كويس جدًا و**مستعمل بصفر** |
| `src/hooks/usePosDraft.ts` | — | ميت (الملفات اللي فوق بس بتستورد الـ type منه) |
| `src/hooks/useSwipe.ts` | — | ميت |
| `src/pages/Dashboard.tsx` | 142 | متعمله route على `/dashboard` بس **مفيش أي لينك** ليه في TopNav ولا MobileNav ولا Sidebar، و`/` بيعمل redirect على `/orders` |

الأسوأ: `src/components/layout/Sidebar.tsx:113`

```tsx
if (!isMobile) {
  return null;   // ← يعني مفيش sidebar على الديسكتوب خالص
}
```

يعني زر الـ collapse والـ `ChevronLeft/Right` والـ `w-20/w-64` والـ user mini-profile
= كل ده كود مش بيشتغل أبدًا. الـ TopNav هو اللي شايل التنقل على الديسكتوب.

**التصليح:** امسح الملفات الميتة، وحوّل الـ pages اللي لسه حية تستخدم `ui/Card.tsx`
بدل ما كل صفحة تعمل panel من عندها.

### 1.4 خمس أشكال مختلفة لنفس الكارت

| المكان | الكلاس |
|---|---|
| `ui/Card.tsx` | `bg-white rounded-2xl border border-gray-200 shadow-sm` |
| `pages/Settings.tsx` | `bg-white rounded-2xl border border-gray-100` |
| `pages/Reports.tsx` | `bg-white p-3 md:p-6 rounded-xl md:rounded-2xl shadow-sm border border-gray-100` |
| `pages/Inventory.tsx` | `bg-white/95 border border-gray-100 rounded-2xl p-5` |
| `POSView.tsx` | `bg-white p-2.5 rounded-2xl border border-gray-200/80` |

النتيجة: `gray-100` و `gray-200` و `gray-200/80` و `white/95` في نفس التطبيق،
و`rounded-xl` على الموبايل و`rounded-2xl` على الديسكتوب في صفحة واحدة.

**التصليح:** استخدم `ui/Card.tsx` في كل مكان. الملف موجود وجاهز.

### 1.5 العملة مكتوبة بثلاث طرق

- `Reports.tsx`: `123.00 ج.م` + `toLocaleString('en-US')`
- `Inventory.tsx`: `EGP 123.00` + `toLocaleString(undefined)`
- `POSView.tsx`: `123.00` وبعده `ج.م` بخط 10px، وبيرجع لـ `isRtl ? 'ج.م' : 'EGP'`
- `InvoicePanel.tsx`: `currency = 'ج.م'` ثابت، و**مش بيحترم الـ isRtl**

يعني نفس الرقم يتكتب بشكلين مختلفين حسب الشاشة، والـ locale مختلف
(`en-US` vs `undefined`) فالفواصل نفسها ممكن تختلف.

**التصليح:** helper واحد `formatMoney(n)` في `utils/` ويتستخدم في كل مكان.

---

## أولوية 2 — مشاكل تنقل وبنية معلومات

### 2.1 الموبايل فيه 4 تبويبات والديسكتوب فيه 6

- `MobileNav.tsx`: كاشير، فواتير، منيو، تقارير
- `TopNav.tsx` / `Sidebar.tsx`: + **المخزون** و **الإعدادات**

يعني على تابلت touch، الموظف مش قادر يوصل للمخزون أو الإعدادات بضغطة واحدة —
لازم يفتح الدرج الأول. على شاشة كاشير حقيقية ده عنق زجاجة.

### 2.2 أرقام سحرية لارتفاع الهيدر

`DashboardLayout.tsx`:

```tsx
pt-[68px] md:pt-2.5   // صفحة الكاشير
pt-[72px] md:pt-6     // باقي الصفحات
```

الرقمين مرتبطين بارتفاع `MobileHeader` اللي هو `fixed`. أي تغيير في حجم الخط
أو إضافة سطر = كل الصفحات تتحرك.
**التصليح:** CSS variable أو قياس فعلي بدل الـ hardcode.

### 2.3 `isMenuDomain()` فحص فضفاض

```ts
return host.startsWith('menu.') || host.includes('menu');
```

`host.includes('menu')` هيمسك أي دومين فيه كلمة menu في أي مكان.
**التصليح:** `host.startsWith('menu.')` وبس.

### 2.4 صفحة Dashboard يتيمه

شاشة حلوة ومعمولة، بس مفيش أي طريق توصلها. يا وصلها (ضيفها في الـ nav)
يا احذفها. وجودها كده بس بيشتت.

---

## أولوية 3 — تحسينات UX لكل شاشة

### 3.1 الكاشير (أهم شاشة في البرنامج)

| # | المشكلة | التصليح المقترح |
|---|---|---|
| 1 | **عمود الدفع بيختفي كله في وضع Dine-in** (`POSView.tsx:610`) — الـ keypad والـ smart cash والمستلم والباقي وطريقة الدفع كلها بتتمسح، والتخطيط يقفز من 3 أعمدة لـ 2. الكاشير بيفقد حاسبة الباقي بالظبط وقت تقسيم فاتورة ترابيزة | خلي العمود mounted واعمله disabled/dim، أو اعرض placeholder بيقول "التحصيل من شاشة الفواتير" |
| 2 | **`alert()` في أهم مسار** (`POSView.tsx:493,498,545,550`) — نافذة native بتقفل شاشة Electron وسط البيعة. وفي نفس الكومبوننت `showToast` + `playWarningSound` جاهزين | استخدم الـ toast الموجود |
| 3 | **أزرار التأكيد صغيرة جدًا**: `py-1.5` على "Print & Pay" و"Save Invoice" ≈ 30px. الـ `.mobile-touch-target` عندك بيقول 44px. وأزرار الكمية `p-1` ≈ 24px | وصّلهم لـ 44px على الأقل |
| 4 | **"Clear / Reset" جنب "Save Invoice" بنفس الوزن البصري ومفيش تأكيد** — ضغطة غلط تمسح الأوردر | خلي الـ reset ثانوي (outline/ghost) + تأكيد لو فيه أصناف |
| 5 | **اختيار الكاشير اختياري وصامت** — الزر برتقالي (تحذير) بس تقدر تحفظ الأوردر عادي | إما إلزامي أو وضح: "مفيش كاشير — الأوردر مش هيتنساب" |
| 6 | **الـ keypad فيه `00` و `.` ومفيش backspace** — لو كتبت 100 بدل 10 لازم `C` وتكتب من الأول | ضيف زر ⌫ |
| 7 | **مفيش أي اختصارات كيبورد** — على كاشير ديسكتوب، مفيش F-key للدفع ولا Esc للمسح ولا Enter للحفظ | أهم حاجة تزود إنتاجية ممكن تعملها |
| 8 | **`text-[9px]` على `ضبط` / `تقريب`** — مش مقروء على شاشة till ويفشل في التباين | `text-[11px]` على الأقل |

### 3.2 التقارير

| # | المشكلة | التصليح |
|---|---|---|
| 9 | **إيموجي كأداة تصميم**: `🎒 ☕ ✅ ⏳ 💵 💳` هم المفتاح البصري الوحيد لـ 6 مقاييس. شكلهم مختلف حسب الـ OS ومفيش تحكم في اللون/التباين | lucide icons زي باقي التطبيق |
| 10 | **8 أرقام في صفين بنفس الحجم بالظبط** — مفيش أي hierarchy. عين المدير ملهاش مكان تحطّ عليه | قسّم لثلاث مجموعات بعناوين (مبيعات / تكلفة / مخزون) أو كبّر الإيرادات وصافي الربح |
| 11 | **زر "Export" بيعمل `window.print()`** — مفيش تصدير ولا CSV | سمّيه "طباعة"، أو اعمل CSV حقيقي (عندك `reports-site/src/csv.ts` تتنسخ منه) |
| 12 | **`aria-label={t('Reports & Analytics')}` على الـ select** — ده اسم الصفحة، المفروض "الفترة" | `aria-label="الفترة الزمنية"` |
| 13 | **"منذ 5 دقائق" بيتحسب بـ `Date.now()` وقت الـ render** — النص بيتجمّد ومش بيتحدث | `setInterval` بسيط أو `useNow()` |

### 3.3 المخزون

| # | المشكلة | التصليح |
|---|---|---|
| 14 | **جدول 9 أعمدة بـ `overflow-x-auto` على الموبايل** — الموظف بيعمل scroll أفقي عبر 4 شاشات أرقام | view كروت تحت `md`، أو خبي الأعمدة الـ 3 بتاعة "المتوقع" ورا toggle |
| 15 | **`text-left` على جدول في تطبيق RTL** — العناوين مش هتتظبط مع القيم | `text-start` |
| 16 | **5 × `alert()`/`confirm()`** (سطور 196، 216، 230، 235، 240) — وفي `ui/Modal.tsx` كومبوننت مودال جاهز الصفحة بتعمل اتنين بإيدها | استخدم `ui/Modal` + `useDialog` |
| 17 | **مقياس المخزون مضلل**: `Math.min(stock/(minStock*2)*100, 100)` — يعني البار لما يوصل 50% ده معناه "وصلت حد التحذير"، مش "نص" | اكتبها صريحة أو اعرض "X يوم تغطية" |

### 3.4 المنيو والمنيو العام

| # | المشكلة | التصليح |
|---|---|---|
| 18 | **الـ PublicMenu مفيهوش loading skeleton** — بيبدّل الصفحة كلها، والـ `min-h-screen` + footer بيخلي الفوتر يطفش في نص الشاشة لو الصفحة قصيرة | skeleton أو `flex flex-col min-h-screen` + `flex-1` على الـ main |
| 19 | **مفيش sticky category bar ولا زر رجوع لفوق** — مع 40+ صنف العميل بيعمل scroll طويل | `position: sticky` على شريط الأقسام |
| 20 | **لغتين مختلفتين بصريًا**: المنيو الداخلي Tailwind utility cards، والـ PublicMenu تجربة gradient/banner ببالته `stone-*`. ده مقبول (جمهورين مختلفين) بس لازم يكون مقصود وموثق | قرار واعي + توثيق |

### 3.5 الإعدادات

| # | المشكلة | التصليح |
|---|---|---|
| 21 | **نصوص عربي hardcoded** (سطور 64-65): `'هوية ومظهر المنيو الإلكتروني'` مش مارّة على `t()` — هتفضل عربي في الوضع الإنجليزي | `t()` زي الباقي |
| 22 | **أنيميشن دخول متدرّج**: `delay: idx * 0.1` على 4 أقسام + 0.3/0.33/0.4 على الباقي = الصفحة تاخد ~0.7 ثانية تكتمل. على شاشة إعدادات المستخدم شافها قبل كده ده مجرد بطء | قلّل أو شيل |
| 23 | **"تسجيل الخروج" زر أحمر بعرض الشاشة ومفيش تأكيد** — ضغطة غلط في نص الشيفت تخرج الكاشير | تأكيد أو نقله للهيدر |

### 3.6 تسجيل الدخول

شاشة كويسة عمومًا: مدمجة، numpad، هوية الفرع باينة. ملاحظتين:
- الـ numpad رقمي بـ `font-mono` بس صف `مسح`/`⌫` بـ `font-cairo` و `text-xs`
  → الصف الأخير شكله مختلف عن باقي الأزرار.
- مفيش أي مخرج ظاهر لـ "نسيت كلمة المرور" / manager override، مع إن
  `needsPasswordSetup` موجود.

---

## بوابة التقارير (`reports-site/`) — الحالة

**أنضف حتة في المشروع.** نظام CSS variables على `:root`، `data-theme` للوضع الليلي،
`data-density` لكثافة الصفوف، مفيش لون مكرر. مفيش حاجة تتصلح بنيويًا.

ملاحظتين بس:
1. البوابة فيها light/dark والتطبيق الرئيسي light-only → البقى شكلهم منتجين مختلفين.
   يا وصّل التطبيق الرئيسي لنفس نظام الـ variables، يا قبّل الاختلاف رسميًا.
2. الرسم البياني مكتوب بإيدك (`chart-bar` / `chart-col` CSS) — شغال ومفيش dependency،
   بس هيحتاج صيانة لو عايز خطوط اتجاه أو محاور.

---

## خطة مقترحة (بالترتيب)

**أسبوع 1 — تصليحات سريعة (مخاطرها صفر، أثرها فوري)**
1. ضيف scale ألوان `caramel` أو صحّح الاستخدام في `Reports.tsx`
2. شيل/عرّف `animate-fade-in`
3. `alert()`/`confirm()` → toast + `ui/Modal` (10 مواقع)
4. `formatMoney()` واحد لكل التطبيق
5. `text-left` → `text-start`، و`aria-label` الـ reports select

**أسبوع 2 — تنضيف**
6. امسح الملفات الميتة (InvoicePanel, PaymentPanel, ProductGrid, Card or use it, usePosDraft, useSwipe)
7. وحّد كل الـ panels على `ui/Card.tsx`
8. قرر مصير `Dashboard.tsx` و `Sidebar` (احذف الكود اللي مش بيشتغل)

**أسبوع 3 — UX حقيقي**
9. الكاشير: أحجام touch 44px، backspace في الـ keypad، اختصارات كيبورد،
   منع اختفاء عمود الدفع في Dine-in، تأكيد على الـ reset
10. MobileNav يوصل لـ 6 أقسام
11. المخزون: view موبايل بدل الـ scroll الأفقي
12. التقارير: hierarchy للأرقام + إيموجي → icons

---

## حاجات شغالة كويس (ما تتلمسش)

- بالتة `mocha` ممتازة ومستخدمة باتساق
- `ui/StatCard.tsx` و `ui/Modal.tsx` + `useDialog` (focus trap, Esc, focus restore) — مبنيين صح
- `PublicMenu.tsx` — معالجة الأخطاء والصور والـ `normalizeMenuConfig` قبل ما توصل لـ CSS كلها احترافية
- `reports-site/src/styles.css` — مثال يُحتذى في تنظيم الـ variables
- `index.css` — الـ `tabular-nums` على الأرقام ومنع الـ text-selection على اللمس تفاصيل مدروسة
